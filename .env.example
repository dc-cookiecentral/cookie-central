VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Dev-only: skip the magic-link login on the local dev server (auto-signs in as
# a mock admin). Only takes effect under `vite dev`; ignored in production builds.
# VITE_AUTH_BYPASS=true
